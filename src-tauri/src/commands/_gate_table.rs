// AUTO-GENERATED by gate_gen.py — 统一门面匹配表。请勿手改。
match module {
    "agent_view" => {
        match action {
            "agent_view_cancel" => {
                let session_id = gstr(args.get("session_id"));
                match super::agent_view_cancel(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_view_config" => {
                match super::agent_view_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_view_events" => {
                let session_id = gstr(args.get("session_id"));
                let count = gopt_usize(args.get("count"));
                match super::agent_view_events(session_id, count) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_view_get" => {
                let id = gstr(args.get("id"));
                match super::agent_view_get(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_view_list" => {
                match super::agent_view_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_view_pause" => {
                let session_id = gstr(args.get("session_id"));
                match super::agent_view_pause(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_view_resume" => {
                let session_id = gstr(args.get("session_id"));
                match super::agent_view_resume(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_view_set_config" => {
                let config = serde_json::from_value::<AgentViewConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::agent_view_set_config(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_view_summary" => {
                match super::agent_view_summary() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_view_tick" => {
                match super::agent_view_tick() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: agent_view: 未知 action: {}", action))
        }
    }
    "annotation" => {
        match action {
            "annotation_collection_create" => {
                let name = gstr(args.get("name"));
                let description = goptstr(args.get("description"));
                let url = gstr(args.get("url"));
                let annotation_ids = gvec(args.get("annotation_ids"));
                match super::annotation_collection_create(name, description, url, annotation_ids) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_collection_delete" => {
                let id = gstr(args.get("id"));
                match super::annotation_collection_delete(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_collection_get" => {
                let id = gstr(args.get("id"));
                match super::annotation_collection_get(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_collection_list" => {
                match super::annotation_collection_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_config" => {
                match super::annotation_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_create" => {
                let url = gstr(args.get("url"));
                let page_title = gstr(args.get("page_title"));
                let selector = gstr(args.get("selector"));
                let highlighted_text = gstr(args.get("highlighted_text"));
                let comment = gstr(args.get("comment"));
                let annotation_type = goptstr(args.get("annotation_type"));
                let tags = gopt_vec(args.get("tags"));
                match super::annotation_create(url, page_title, selector, highlighted_text, comment, annotation_type, tags) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_delete" => {
                let id = gstr(args.get("id"));
                match super::annotation_delete(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_get" => {
                let id = gstr(args.get("id"));
                match super::annotation_get(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_get_for_url" => {
                let url = gstr(args.get("url"));
                match super::annotation_get_for_url(url) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_list" => {
                let url = goptstr(args.get("url"));
                let resolved = gob(args.get("resolved"));
                let page = gopt_u32(args.get("page"));
                match super::annotation_list(url, resolved, page) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_resolve" => {
                let id = gstr(args.get("id"));
                match super::annotation_resolve(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_search" => {
                let query = gstr(args.get("query"));
                match super::annotation_search(query) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_set_config" => {
                let config = serde_json::from_value::<AnnotationConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::annotation_set_config(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_stats" => {
                match super::annotation_stats() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_unresolve" => {
                let id = gstr(args.get("id"));
                match super::annotation_unresolve(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "annotation_update" => {
                let id = gstr(args.get("id"));
                let comment = goptstr(args.get("comment"));
                let annotation_type = goptstr(args.get("annotation_type"));
                let tags = gopt_vec(args.get("tags"));
                match super::annotation_update(id, comment, annotation_type, tags) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: annotation: 未知 action: {}", action))
        }
    }
    "background" => {
        match action {
            "create_background_task" => {
                let name = gstr(args.get("name"));
                let prompt = gstr(args.get("prompt"));
                let schedule = gstr(args.get("schedule"));
                Ok(serde_json::to_value(&super::create_background_task(name, prompt, schedule)).map_err(|e| e.to_string())?)
            }
            "delete_background_task" => {
                let id = gstr(args.get("id"));
                match super::delete_background_task(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "get_background_task_log" => {
                let id = gstr(args.get("id"));
                match super::get_background_task_log(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "list_background_tasks" => {
                Ok(serde_json::to_value(&super::list_background_tasks()).map_err(|e| e.to_string())?)
            }
            "pause_background_task" => {
                let id = gstr(args.get("id"));
                match super::pause_background_task(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "resume_background_task" => {
                let id = gstr(args.get("id"));
                match super::resume_background_task(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "run_background_task_now" => {
                let id = gstr(args.get("id"));
                match super::run_background_task_now(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: background: 未知 action: {}", action))
        }
    }
    "channels" => {
        match action {
            "channels_add" => {
                let channel_type = gstr(args.get("channel_type"));
                let name = gstr(args.get("name"));
                let webhook_url = gstr(args.get("webhook_url"));
                match super::channels_add(channel_type, name, webhook_url) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "channels_disable" => {
                let id = gstr(args.get("id"));
                match super::channels_disable(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "channels_enable" => {
                let id = gstr(args.get("id"));
                match super::channels_enable(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "channels_history" => {
                let count = gus(args.get("count"));
                match super::channels_history(count) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "channels_link_session" => {
                let channel_id = gstr(args.get("channel_id"));
                let session_id = gstr(args.get("session_id"));
                match super::channels_link_session(channel_id, session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "channels_list" => {
                match super::channels_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "channels_receive" => {
                let channel_id = gstr(args.get("channel_id"));
                match super::channels_receive(channel_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "channels_remove" => {
                let id = gstr(args.get("id"));
                match super::channels_remove(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "channels_send" => {
                let channel_id = gstr(args.get("channel_id"));
                let content = gstr(args.get("content"));
                match super::channels_send(channel_id, content) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "channels_unlink_session" => {
                let channel_id = gstr(args.get("channel_id"));
                match super::channels_unlink_session(channel_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "slack_config" => {
                match super::slack_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "slack_configure" => {
                let workspace = gstr(args.get("workspace"));
                let channel = gstr(args.get("channel"));
                let token = gstr(args.get("token"));
                match super::slack_configure(workspace, channel, token) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "slack_send" => {
                let message = gstr(args.get("message"));
                match super::slack_send(message) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "slack_status" => {
                match super::slack_status() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: channels: 未知 action: {}", action))
        }
    }
    "chat" => {
        match action {
            "list_conversations" => {
                Ok(serde_json::to_value(&super::list_conversations()).map_err(|e| e.to_string())?)
            }
            "stop_generation" => {
                match super::stop_generation() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: chat: 未知 action: {}", action))
        }
    }
    "computer" => {
        match action {
            "capture_screen" => {
                match super::capture_screen() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "get_frontmost_app" => {
                match super::get_frontmost_app() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "get_window_list" => {
                match super::get_window_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: computer: 未知 action: {}", action))
        }
    }
    "computer_interactive" => {
        match action {
            "computer_bg_cancel" => {
                let task_id = gstr(args.get("task_id"));
                match super::computer_bg_cancel(task_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "computer_bg_clear" => {
                let include_running = gob(args.get("include_running"));
                match super::computer_bg_clear(include_running) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "computer_bg_config" => {
                match super::computer_bg_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "computer_bg_get" => {
                let task_id = gstr(args.get("task_id"));
                match super::computer_bg_get(task_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "computer_bg_list" => {
                let status = goptstr(args.get("status"));
                match super::computer_bg_list(status) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "computer_bg_retry" => {
                let task_id = gstr(args.get("task_id"));
                match super::computer_bg_retry(task_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "computer_bg_run_script" => {
                let script_name = gstr(args.get("script_name"));
                let args = gopt_vec(args.get("args"));
                match super::computer_bg_run_script(script_name, args) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "computer_bg_set_config" => {
                let config = serde_json::from_value::<BackgroundTaskConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::computer_bg_set_config(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "computer_bg_stats" => {
                match super::computer_bg_stats() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "computer_bg_submit" => {
                let type_ = gstr(args.get("type_"));
                let target = gstr(args.get("target"));
                let params = gopt_json(args.get("params"));
                match super::computer_bg_submit(type_, target, params) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "computer_screen_capture" => {
                let path = goptstr(args.get("path"));
                match super::computer_screen_capture(path) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: computer_interactive: 未知 action: {}", action))
        }
    }
    "context" => {
        match action {
            "context_analyze" => {
                let session_id = gstr(args.get("session_id"));
                match super::context_analyze(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "context_check_threshold" => {
                let session_id = gstr(args.get("session_id"));
                match super::context_check_threshold(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "context_compact" => {
                let session_id = gstr(args.get("session_id"));
                let level = goptstr(args.get("level"));
                let strategy = goptstr(args.get("strategy"));
                match super::context_compact(session_id, level, strategy) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "context_config" => {
                match super::context_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "context_expand" => {
                match super::context_expand() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "context_extract_decisions" => {
                let text = gstr(args.get("text"));
                match super::context_extract_decisions(text) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "context_get_segment" => {
                let segment_id = gstr(args.get("segment_id"));
                match super::context_get_segment(segment_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "context_get_segments" => {
                let session_id = gstr(args.get("session_id"));
                match super::context_get_segments(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "context_set_config" => {
                let config = serde_json::from_value::<CompactionConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::context_set_config(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "context_stats" => {
                match super::context_stats() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "context_summarize" => {
                let text = gstr(args.get("text"));
                let max_chars = gopt_u32(args.get("max_chars"));
                match super::context_summarize(text, max_chars) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: context: 未知 action: {}", action))
        }
    }
    "coordinator" => {
        match action {
            "coordinator_list" => {
                match super::coordinator_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "coordinator_remove" => {
                let worker_id = gstr(args.get("worker_id"));
                match super::coordinator_remove(worker_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "coordinator_set_max_workers" => {
                let max = gus(args.get("max"));
                match super::coordinator_set_max_workers(max) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "coordinator_set_strategy" => {
                let strategy = gstr(args.get("strategy"));
                match super::coordinator_set_strategy(strategy) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "coordinator_spawn" => {
                let task = gstr(args.get("task"));
                match super::coordinator_spawn(task) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "coordinator_update" => {
                let worker_id = gstr(args.get("worker_id"));
                let progress = gf64(args.get("progress"));
                let status = gstr(args.get("status"));
                match super::coordinator_update(worker_id, progress, status) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: coordinator: 未知 action: {}", action))
        }
    }
    "daemon" => {
        match action {
            "daemon_auto_fix" => {
                let path = gstr(args.get("path"));
                match super::daemon_auto_fix(path) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "daemon_log" => {
                let count = gus(args.get("count"));
                Ok(serde_json::to_value(&super::daemon_log(count)).map_err(|e| e.to_string())?)
            }
            "daemon_start" => {
                let config = serde_json::from_value::<DaemonConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::daemon_start(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "daemon_status" => {
                match super::daemon_status() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "daemon_stop" => {
                match super::daemon_stop() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: daemon: 未知 action: {}", action))
        }
    }
    "desktop" => {
        match action {
            "read_clipboard" => {
                match super::read_clipboard() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "switch_app" => {
                let app_name = gstr(args.get("app_name"));
                match super::switch_app(app_name) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "write_clipboard" => {
                let text = gstr(args.get("text"));
                match super::write_clipboard(text) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: desktop: 未知 action: {}", action))
        }
    }
    "dream" => {
        match action {
            "dream_consolidate_now" => {
                match super::dream_consolidate_now() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "dream_entries" => {
                Ok(serde_json::to_value(&super::dream_entries()).map_err(|e| e.to_string())?)
            }
            "dream_start" => {
                let config = serde_json::from_value::<DreamConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::dream_start(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "dream_status" => {
                match super::dream_status() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "dream_stop" => {
                match super::dream_stop() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: dream: 未知 action: {}", action))
        }
    }
    "enterprise" => {
        match action {
            "api_call" => {
                let name = gstr(args.get("name"));
                let body = goptstr(args.get("body"));
                match super::api_call(name, body) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "api_config" => {
                match super::api_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "api_delete" => {
                let id = gstr(args.get("id"));
                match super::api_delete(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "api_list" => {
                match super::api_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "api_register" => {
                let name = gstr(args.get("name"));
                let url = gstr(args.get("url"));
                let method = gstr(args.get("method"));
                match super::api_register(name, url, method) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "api_test" => {
                let id = gstr(args.get("id"));
                match super::api_test(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "enterprise_audit_log" => {
                let count = gus(args.get("count"));
                match super::enterprise_audit_log(count) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "enterprise_audit_log_action" => {
                let action = gstr(args.get("action"));
                let actor = gstr(args.get("actor"));
                let detail = gstr(args.get("detail"));
                match super::enterprise_audit_log_action(action, actor, detail) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "enterprise_compliance_check" => {
                match super::enterprise_compliance_check() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "enterprise_delete_policy" => {
                let key = gstr(args.get("key"));
                match super::enterprise_delete_policy(key) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "enterprise_license_info" => {
                match super::enterprise_license_info() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "enterprise_list_policies" => {
                match super::enterprise_list_policies() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "enterprise_set_policy" => {
                let key = gstr(args.get("key"));
                let value = gstr(args.get("value"));
                let description = gstr(args.get("description"));
                let enforced = gb(args.get("enforced"));
                match super::enterprise_set_policy(key, value, description, enforced) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "enterprise_status" => {
                match super::enterprise_status() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: enterprise: 未知 action: {}", action))
        }
    }
    "gate" => {
        match action {
            "gate_approve" => {
                let override_reason = gstr(args.get("override_reason"));
                match super::gate_approve(override_reason) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "gate_audit_log" => {
                let count = gus(args.get("count"));
                Ok(serde_json::to_value(&super::gate_audit_log(count)).map_err(|e| e.to_string())?)
            }
            "gate_get_config" => {
                match super::gate_get_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "gate_get_policy" => {
                match super::gate_get_policy() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "gate_run_check" => {
                let path = gstr(args.get("path"));
                match super::gate_run_check(path) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "gate_set_config" => {
                let config = serde_json::from_value::<GateConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::gate_set_config(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "gate_set_policy" => {
                let policy = serde_json::from_value::<GatePolicy>(gjson(args.get("policy"))).map_err(|e| e.to_string())?;
                match super::gate_set_policy(policy) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: gate: 未知 action: {}", action))
        }
    }
    "insights" => {
        match action {
            "insights_card_get" => {
                let id = gstr(args.get("id"));
                match super::insights_card_get(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "insights_card_list" => {
                match super::insights_card_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "insights_card_share" => {
                let id = gstr(args.get("id"));
                match super::insights_card_share(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "insights_config" => {
                match super::insights_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "insights_daily" => {
                let date = goptstr(args.get("date"));
                match super::insights_daily(date) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "insights_generate_card" => {
                let period = goptstr(args.get("period"));
                let theme = goptstr(args.get("theme"));
                let title = goptstr(args.get("title"));
                match super::insights_generate_card(period, theme, title) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "insights_insights" => {
                let period = goptstr(args.get("period"));
                match super::insights_insights(period) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "insights_record_event" => {
                let event_type = gstr(args.get("event_type"));
                let details = gstr(args.get("details"));
                let session_id = goptstr(args.get("session_id"));
                let project = goptstr(args.get("project"));
                let duration_ms = gopt_u32(args.get("duration_ms"));
                match super::insights_record_event(event_type, details, session_id, project, duration_ms) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "insights_reset" => {
                match super::insights_reset() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "insights_set_config" => {
                let config = serde_json::from_value::<InsightsConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::insights_set_config(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "insights_stats" => {
                match super::insights_stats() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "insights_trend" => {
                let days = gopt_u32(args.get("days"));
                match super::insights_trend(days) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "insights_weekly" => {
                let week_start = goptstr(args.get("week_start"));
                match super::insights_weekly(week_start) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: insights: 未知 action: {}", action))
        }
    }
    "marketplace" => {
        match action {
            "marketplace_categories" => {
                match super::marketplace_categories() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "marketplace_check_updates" => {
                match super::marketplace_check_updates() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "marketplace_config" => {
                match super::marketplace_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "marketplace_featured" => {
                match super::marketplace_featured() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "marketplace_get" => {
                let plugin_id = gstr(args.get("plugin_id"));
                match super::marketplace_get(plugin_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "marketplace_install" => {
                let plugin_id = gstr(args.get("plugin_id"));
                match super::marketplace_install(plugin_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "marketplace_list" => {
                let category = goptstr(args.get("category"));
                let page = gopt_u32(args.get("page"));
                let sort = goptstr(args.get("sort"));
                match super::marketplace_list(category, page, sort) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "marketplace_reviews" => {
                let plugin_id = gstr(args.get("plugin_id"));
                match super::marketplace_reviews(plugin_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "marketplace_search" => {
                let query = gstr(args.get("query"));
                let category = goptstr(args.get("category"));
                let page = gopt_u32(args.get("page"));
                match super::marketplace_search(query, category, page) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "marketplace_set_config" => {
                let config = serde_json::from_value::<MarketplaceConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::marketplace_set_config(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "marketplace_stats" => {
                match super::marketplace_stats() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "marketplace_submit_review" => {
                let plugin_id = gstr(args.get("plugin_id"));
                let rating = gu8(args.get("rating"));
                let title = gstr(args.get("title"));
                let body = gstr(args.get("body"));
                match super::marketplace_submit_review(plugin_id, rating, title, body) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "marketplace_uninstall" => {
                let plugin_id = gstr(args.get("plugin_id"));
                match super::marketplace_uninstall(plugin_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "marketplace_update" => {
                let plugin_id = gstr(args.get("plugin_id"));
                match super::marketplace_update(plugin_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "marketplace_update_all" => {
                match super::marketplace_update_all() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: marketplace: 未知 action: {}", action))
        }
    }
    "mcp_host" => {
        match action {
            "mcp_host_list_endpoints" => {
                match super::mcp_host_list_endpoints() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "mcp_host_log" => {
                let count = gus(args.get("count"));
                match super::mcp_host_log(count) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "mcp_host_ping" => {
                match super::mcp_host_ping() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "mcp_host_register_endpoint" => {
                let name = gstr(args.get("name"));
                let description = gstr(args.get("description"));
                let params = gvec(args.get("params"));
                match super::mcp_host_register_endpoint(name, description, params) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "mcp_host_sessions" => {
                match super::mcp_host_sessions() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "mcp_host_start" => {
                let config = serde_json::from_value::<McpHostConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::mcp_host_start(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "mcp_host_status" => {
                match super::mcp_host_status() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "mcp_host_stop" => {
                match super::mcp_host_stop() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "mcp_host_unregister_endpoint" => {
                let name = gstr(args.get("name"));
                match super::mcp_host_unregister_endpoint(name) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: mcp_host: 未知 action: {}", action))
        }
    }
    "neocodex" => {
        match action {
            "neocodex_git_file_status" => {
                let cwd = goptstr(args.get("cwd"));
                match super::neocodex_git_file_status(cwd) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: neocodex: 未知 action: {}", action))
        }
    }
    "pet" => {
        match action {
            "get_pet_state" => {
                Ok(serde_json::to_value(&super::get_pet_state()).map_err(|e| e.to_string())?)
            }
            _ => Err(format!("unified_invoke: pet: 未知 action: {}", action))
        }
    }
    "plugin" => {
        match action {
            "plugin_config" => {
                match super::plugin_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "plugin_get" => {
                let id = gstr(args.get("id"));
                match super::plugin_get(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "plugin_run" => {
                let id = gstr(args.get("id"));
                let entry_point = gstr(args.get("entry_point"));
                let args = gopt_vec(args.get("args"));
                match super::plugin_run(id, entry_point, args) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "plugin_set_config" => {
                let config = serde_json::from_value::<PluginConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::plugin_set_config(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: plugin: 未知 action: {}", action))
        }
    }
    "preview" => {
        match action {
            "chrome_debug_capture_screenshot" => {
                match super::chrome_debug_capture_screenshot() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "chrome_debug_clear_console_logs" => {
                match super::chrome_debug_clear_console_logs() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "chrome_debug_disconnect" => {
                match super::chrome_debug_disconnect() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "chrome_debug_evaluate" => {
                let expression = gstr(args.get("expression"));
                match super::chrome_debug_evaluate(expression) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "chrome_debug_get_console_logs" => {
                match super::chrome_debug_get_console_logs() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "chrome_debug_list_targets" => {
                match super::chrome_debug_list_targets() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "chrome_debug_navigate" => {
                let url = gstr(args.get("url"));
                match super::chrome_debug_navigate(url) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "chrome_debug_reload" => {
                match super::chrome_debug_reload() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "chrome_debug_status" => {
                match super::chrome_debug_status() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "preview_config" => {
                match super::preview_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "preview_list" => {
                match super::preview_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "preview_navigate" => {
                let session_id = gstr(args.get("session_id"));
                let url = gstr(args.get("url"));
                match super::preview_navigate(session_id, url) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "preview_reload" => {
                let session_id = gstr(args.get("session_id"));
                match super::preview_reload(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "preview_screenshot" => {
                let session_id = gstr(args.get("session_id"));
                match super::preview_screenshot(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "preview_set_config" => {
                let config = serde_json::from_value::<PreviewConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::preview_set_config(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "preview_start" => {
                let url = gstr(args.get("url"));
                let width = gopt_u32(args.get("width"));
                let height = gopt_u32(args.get("height"));
                match super::preview_start(url, width, height) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "preview_stop" => {
                let session_id = gstr(args.get("session_id"));
                match super::preview_stop(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: preview: 未知 action: {}", action))
        }
    }
    "remote" => {
        match action {
            "add_remote_host" => {
                let name = gstr(args.get("name"));
                let host = gstr(args.get("host"));
                let port = gu16(args.get("port"));
                let user = gstr(args.get("user"));
                let auth_method = gstr(args.get("auth_method"));
                let key_path = goptstr(args.get("key_path"));
                Ok(serde_json::to_value(&super::add_remote_host(name, host, port, user, auth_method, key_path)).map_err(|e| e.to_string())?)
            }
            "execute_remote" => {
                let id = gstr(args.get("id"));
                let command = gstr(args.get("command"));
                match super::execute_remote(id, command) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "list_remote_hosts" => {
                Ok(serde_json::to_value(&super::list_remote_hosts()).map_err(|e| e.to_string())?)
            }
            "remove_remote_host" => {
                let id = gstr(args.get("id"));
                match super::remove_remote_host(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "test_remote_connection" => {
                let id = gstr(args.get("id"));
                match super::test_remote_connection(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: remote: 未知 action: {}", action))
        }
    }
    "remote_bridge" => {
        match action {
            "remote_bridge_broadcast" => {
                let kind = gstr(args.get("kind"));
                let payload = gstr(args.get("payload"));
                match super::remote_bridge_broadcast(kind, payload) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "remote_bridge_connect" => {
                let device_name = gstr(args.get("device_name"));
                let device_type = gstr(args.get("device_type"));
                let code = gstr(args.get("code"));
                match super::remote_bridge_connect(device_name, device_type, code) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "remote_bridge_devices" => {
                Ok(serde_json::to_value(&super::remote_bridge_devices()).map_err(|e| e.to_string())?)
            }
            "remote_bridge_disconnect" => {
                let device_id = gstr(args.get("device_id"));
                match super::remote_bridge_disconnect(device_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "remote_bridge_history" => {
                let count = gus(args.get("count"));
                Ok(serde_json::to_value(&super::remote_bridge_history(count)).map_err(|e| e.to_string())?)
            }
            "remote_bridge_pair" => {
                match super::remote_bridge_pair() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "remote_bridge_poll" => {
                let device_id = gstr(args.get("device_id"));
                Ok(serde_json::to_value(&super::remote_bridge_poll(device_id)).map_err(|e| e.to_string())?)
            }
            "remote_bridge_send" => {
                let device_id = gstr(args.get("device_id"));
                let kind = gstr(args.get("kind"));
                let payload = gstr(args.get("payload"));
                match super::remote_bridge_send(device_id, kind, payload) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "remote_bridge_status" => {
                match super::remote_bridge_status() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: remote_bridge: 未知 action: {}", action))
        }
    }
    "review" => {
        match action {
            "review_diff" => {
                let diff_content = gstr(args.get("diff_content"));
                let pr_title = gstr(args.get("pr_title"));
                match super::review_diff(diff_content, pr_title) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "review_get_issue_detail" => {
                let file_path = gstr(args.get("file_path"));
                let line = gus(args.get("line"));
                match super::review_get_issue_detail(file_path, line) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: review: 未知 action: {}", action))
        }
    }
    "security_scan" => {
        match action {
            "security_scan_apply_patch" => {
                let finding_id = gstr(args.get("finding_id"));
                match super::security_scan_apply_patch(finding_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "security_scan_config" => {
                match super::security_scan_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "security_scan_finding_detail" => {
                let finding_id = gstr(args.get("finding_id"));
                match super::security_scan_finding_detail(finding_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "security_scan_findings" => {
                let scan_id = gstr(args.get("scan_id"));
                let severity = goptstr(args.get("severity"));
                match super::security_scan_findings(scan_id, severity) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "security_scan_fix_all" => {
                let finding_ids = gvec(args.get("finding_ids"));
                match super::security_scan_fix_all(finding_ids) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "security_scan_list" => {
                match super::security_scan_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "security_scan_mark_status" => {
                let finding_id = gstr(args.get("finding_id"));
                let status = gstr(args.get("status"));
                match super::security_scan_mark_status(finding_id, status) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "security_scan_quick_check" => {
                match super::security_scan_quick_check() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "security_scan_set_config" => {
                let config = serde_json::from_value::<SecurityScanConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::security_scan_set_config(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "security_scan_start" => {
                let target_path = gstr(args.get("target_path"));
                let depth = goptstr(args.get("depth"));
                match super::security_scan_start(target_path, depth) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "security_scan_status" => {
                let scan_id = gstr(args.get("scan_id"));
                match super::security_scan_status(scan_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "security_scan_summary" => {
                match super::security_scan_summary() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: security_scan: 未知 action: {}", action))
        }
    }
    "skill" => {
        match action {
            "skill_get" => {
                let name = gstr(args.get("name"));
                match super::skill_get(name) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "skill_list" => {
                match super::skill_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "skill_read" => {
                let name = gstr(args.get("name"));
                let file = gstr(args.get("file"));
                match super::skill_read(name, file) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "skill_search" => {
                let query = gstr(args.get("query"));
                match super::skill_search(query) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: skill: 未知 action: {}", action))
        }
    }
    "teleport" => {
        match action {
            "agent_team_add_member" => {
                let team_id = gstr(args.get("team_id"));
                let role = gstr(args.get("role"));
                let task = gstr(args.get("task"));
                match super::agent_team_add_member(team_id, role, task) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_team_complete_member" => {
                let member_id = gstr(args.get("member_id"));
                let result = gstr(args.get("result"));
                match super::agent_team_complete_member(member_id, result) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_team_create" => {
                let name = gstr(args.get("name"));
                let description = gstr(args.get("description"));
                let strategy = gstr(args.get("strategy"));
                match super::agent_team_create(name, description, strategy) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_team_fail_member" => {
                let member_id = gstr(args.get("member_id"));
                let error = gstr(args.get("error"));
                match super::agent_team_fail_member(member_id, error) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_team_get" => {
                let id = gstr(args.get("id"));
                match super::agent_team_get(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_team_list" => {
                match super::agent_team_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_team_messages" => {
                let team_id = gstr(args.get("team_id"));
                match super::agent_team_messages(team_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_team_remove_member" => {
                let team_id = gstr(args.get("team_id"));
                let member_id = gstr(args.get("member_id"));
                match super::agent_team_remove_member(team_id, member_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_team_result" => {
                let team_id = gstr(args.get("team_id"));
                match super::agent_team_result(team_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_team_send_message" => {
                let team_id = gstr(args.get("team_id"));
                let to = gstr(args.get("to"));
                let content = gstr(args.get("content"));
                let kind = gstr(args.get("kind"));
                match super::agent_team_send_message(team_id, to, content, kind) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_team_start" => {
                let team_id = gstr(args.get("team_id"));
                match super::agent_team_start(team_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "agent_team_status" => {
                let team_id = gstr(args.get("team_id"));
                match super::agent_team_status(team_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "teleport_claim" => {
                let code = gstr(args.get("code"));
                let destination = gstr(args.get("destination"));
                match super::teleport_claim(code, destination) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "teleport_config" => {
                match super::teleport_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "teleport_create" => {
                let source = gstr(args.get("source"));
                let session_data = gstr(args.get("session_data"));
                match super::teleport_create(source, session_data) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "teleport_list" => {
                match super::teleport_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "teleport_revoke" => {
                let session_id = gstr(args.get("session_id"));
                match super::teleport_revoke(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "teleport_set_config" => {
                let config = serde_json::from_value::<TeleportConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::teleport_set_config(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: teleport: 未知 action: {}", action))
        }
    }
    "term_tabs" => {
        match action {
            "term_tabs_activate" => {
                let tab_id = gstr(args.get("tab_id"));
                match super::term_tabs_activate(tab_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "term_tabs_close" => {
                let tab_id = gstr(args.get("tab_id"));
                match super::term_tabs_close(tab_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "term_tabs_config" => {
                match super::term_tabs_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "term_tabs_create" => {
                let session_id = gstr(args.get("session_id"));
                let name = goptstr(args.get("name"));
                let cwd = goptstr(args.get("cwd"));
                let shell = goptstr(args.get("shell"));
                let color = goptstr(args.get("color"));
                match super::term_tabs_create(session_id, name, cwd, shell, color) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "term_tabs_get" => {
                let tab_id = gstr(args.get("tab_id"));
                match super::term_tabs_get(tab_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "term_tabs_group_create" => {
                let name = gstr(args.get("name"));
                let session_id = gstr(args.get("session_id"));
                let tab_ids = gvec(args.get("tab_ids"));
                match super::term_tabs_group_create(name, session_id, tab_ids) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "term_tabs_group_delete" => {
                let group_id = gstr(args.get("group_id"));
                match super::term_tabs_group_delete(group_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "term_tabs_group_list" => {
                let session_id = gstr(args.get("session_id"));
                match super::term_tabs_group_list(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "term_tabs_layout" => {
                let session_id = gstr(args.get("session_id"));
                match super::term_tabs_layout(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "term_tabs_list" => {
                let session_id = gstr(args.get("session_id"));
                match super::term_tabs_list(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "term_tabs_rename" => {
                let tab_id = gstr(args.get("tab_id"));
                let name = gstr(args.get("name"));
                match super::term_tabs_rename(tab_id, name) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "term_tabs_reorder" => {
                let session_id = gstr(args.get("session_id"));
                let tab_ids = gvec(args.get("tab_ids"));
                match super::term_tabs_reorder(session_id, tab_ids) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "term_tabs_set_color" => {
                let tab_id = gstr(args.get("tab_id"));
                let color = gstr(args.get("color"));
                match super::term_tabs_set_color(tab_id, color) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "term_tabs_set_config" => {
                let config = serde_json::from_value::<TerminalTabConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::term_tabs_set_config(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "term_tabs_stats" => {
                match super::term_tabs_stats() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: term_tabs: 未知 action: {}", action))
        }
    }
    "tool" => {
        match action {
            "tool_execute" => {
                let tool = gstr(args.get("tool"));
                let args = gjson(args.get("args"));
                Ok(serde_json::to_value(&super::tool_execute(tool, args)).map_err(|e| e.to_string())?)
            }
            "tool_search" => {
                let query = gstr(args.get("query"));
                let count = gopt_usize(args.get("count"));
                match super::tool_search(query, count) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: tool: 未知 action: {}", action))
        }
    }
    "undercover" => {
        match action {
            "undercover_activate_profile" => {
                let name = gstr(args.get("name"));
                match super::undercover_activate_profile(name) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "undercover_commit_log" => {
                let count = gus(args.get("count"));
                Ok(serde_json::to_value(&super::undercover_commit_log(count)).map_err(|e| e.to_string())?)
            }
            "undercover_get_profiles" => {
                match super::undercover_get_profiles() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "undercover_set_profile" => {
                let name = gstr(args.get("name"));
                let email = gstr(args.get("email"));
                let strip_co_auth = gb(args.get("strip_co_auth"));
                match super::undercover_set_profile(name, email, strip_co_auth) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "undercover_status" => {
                match super::undercover_status() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "undercover_strip_metadata" => {
                let file_path = gstr(args.get("file_path"));
                match super::undercover_strip_metadata(file_path) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "undercover_verify_anonymity" => {
                let path = gstr(args.get("path"));
                match super::undercover_verify_anonymity(path) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: undercover: 未知 action: {}", action))
        }
    }
    "unified" => {
        match action {
            "unified_cli_execute" => {
                let input = gstr(args.get("input"));
                match super::unified_cli_execute(input) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_cli_list" => {
                match super::unified_cli_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_cli_lookup" => {
                let name = gstr(args.get("name"));
                match super::unified_cli_lookup(name) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_command_catalog" => {
                match super::unified_command_catalog() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_tauri_list" => {
                match super::unified_tauri_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: unified: 未知 action: {}", action))
        }
    }
    "unified_session" => {
        match action {
            "unified_session_connect" => {
                let id = gstr(args.get("id"));
                match super::unified_session_connect(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_session_disconnect" => {
                let id = gstr(args.get("id"));
                match super::unified_session_disconnect(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_session_export" => {
                match super::unified_session_export() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_session_get" => {
                let id = gstr(args.get("id"));
                match super::unified_session_get(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_session_group_by" => {
                let field = gstr(args.get("field"));
                match super::unified_session_group_by(field) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_session_import" => {
                let data = gstr(args.get("data"));
                match super::unified_session_import(data) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_session_list" => {
                let filter = {
                let v = args.get("filter");
                if v.map_or(true, |x| x.is_null()) { None }
                else { Some(serde_json::from_value::<UnifiedSessionFilter>(v.cloned().unwrap_or(Value::Null)).map_err(|e| e.to_string())?) }
            };
                match super::unified_session_list(filter) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_session_refresh" => {
                match super::unified_session_refresh() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_session_search" => {
                let query = gstr(args.get("query"));
                match super::unified_session_search(query) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_session_stats" => {
                match super::unified_session_stats() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_session_summary" => {
                match super::unified_session_summary() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_session_tag" => {
                let id = gstr(args.get("id"));
                let tags = gvec(args.get("tags"));
                match super::unified_session_tag(id, tags) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "unified_session_untag" => {
                let id = gstr(args.get("id"));
                let tags = gvec(args.get("tags"));
                match super::unified_session_untag(id, tags) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: unified_session: 未知 action: {}", action))
        }
    }
    "voice" => {
        match action {
            "voice_config" => {
                match super::voice_config() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "voice_execute_command" => {
                let transcript = gstr(args.get("transcript"));
                match super::voice_execute_command(transcript) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "voice_list_sessions" => {
                match super::voice_list_sessions() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "voice_send_audio" => {
                let session_id = gstr(args.get("session_id"));
                let audio_data = gstr(args.get("audio_data"));
                match super::voice_send_audio(session_id, audio_data) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "voice_session_history" => {
                let session_id = gstr(args.get("session_id"));
                match super::voice_session_history(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "voice_session_status" => {
                let session_id = gstr(args.get("session_id"));
                match super::voice_session_status(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "voice_set_config" => {
                let config = serde_json::from_value::<VoiceConfig>(gjson(args.get("config"))).map_err(|e| e.to_string())?;
                match super::voice_set_config(config) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "voice_start_session" => {
                let language = goptstr(args.get("language"));
                match super::voice_start_session(language) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "voice_stats" => {
                match super::voice_stats() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "voice_stop_session" => {
                let session_id = gstr(args.get("session_id"));
                match super::voice_stop_session(session_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "voice_synthesize" => {
                let text = gstr(args.get("text"));
                let voice = goptstr(args.get("voice"));
                match super::voice_synthesize(text, voice) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "voice_test_microphone" => {
                match super::voice_test_microphone() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: voice: 未知 action: {}", action))
        }
    }
    "workflow" => {
        match action {
            "workflow_batch_run" => {
                let workflow_ids = gvec(args.get("workflow_ids"));
                let parallel = gob(args.get("parallel"));
                match super::workflow_batch_run(workflow_ids, parallel) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_create" => {
                let name = gstr(args.get("name"));
                let description = gstr(args.get("description"));
                let steps_json = gstr(args.get("steps_json"));
                match super::workflow_create(name, description, steps_json) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_delete" => {
                let id = gstr(args.get("id"));
                match super::workflow_delete(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_duplicate" => {
                let id = gstr(args.get("id"));
                let new_name = gstr(args.get("new_name"));
                match super::workflow_duplicate(id, new_name) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_export" => {
                let id = gstr(args.get("id"));
                let format = goptstr(args.get("format"));
                match super::workflow_export(id, format) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_generate" => {
                let description = gstr(args.get("description"));
                match super::workflow_generate(description) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_generate_suggest" => {
                let description = gstr(args.get("description"));
                match super::workflow_generate_suggest(description) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_get" => {
                let id = gstr(args.get("id"));
                match super::workflow_get(id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_import_from_json" => {
                let json = gstr(args.get("json"));
                match super::workflow_import_from_json(json) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_list" => {
                match super::workflow_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_run" => {
                let workflow_id = gstr(args.get("workflow_id"));
                match super::workflow_run(workflow_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_run_cancel" => {
                let run_id = gstr(args.get("run_id"));
                match super::workflow_run_cancel(run_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_run_list" => {
                let workflow_id = gstr(args.get("workflow_id"));
                match super::workflow_run_list(workflow_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_run_status" => {
                let run_id = gstr(args.get("run_id"));
                match super::workflow_run_status(run_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_run_steps" => {
                let run_id = gstr(args.get("run_id"));
                match super::workflow_run_steps(run_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_schedule_create" => {
                let workflow_id = gstr(args.get("workflow_id"));
                let trigger = gstr(args.get("trigger"));
                let cron_expr = goptstr(args.get("cron_expr"));
                match super::workflow_schedule_create(workflow_id, trigger, cron_expr) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_schedule_delete" => {
                let schedule_id = gstr(args.get("schedule_id"));
                match super::workflow_schedule_delete(schedule_id) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_schedule_list" => {
                match super::workflow_schedule_list() {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            "workflow_update" => {
                let id = gstr(args.get("id"));
                let name = goptstr(args.get("name"));
                let description = goptstr(args.get("description"));
                let steps_json = goptstr(args.get("steps_json"));
                match super::workflow_update(id, name, description, steps_json) {
                    Ok(v) => Ok(serde_json::to_value(v).map_err(|e| e.to_string())?),
                    Err(e) => Err(e.to_string()),
                }
            }
            _ => Err(format!("unified_invoke: workflow: 未知 action: {}", action))
        }
    }
    _ => Err("unified_invoke: 未知 module".into())
}

# Cleanup 235 — 跨域引用检查

**日期**: 2026-09-11
**范围**: L1-L6 六层架构跨域依赖分析

## 跨域引用矩阵

| 源域 → 目标域 | 引用次数 |
|---------------|----------|
| l5_cognition → l1_action | 2 |
| l5_cognition → l2_perception | 1 |
| l5_cognition → l6_meta | 2 |

## 分析

### 总体状况

**跨域引用极少**，仅 l5_cognition 存在 3 处跨域引用（排除 test/facade 后）。六层架构的依赖方向基本正确。

### l5_cognition → l1_action (2处)

认知层引用行动层。可能的合理场景：
- 认知层触发动作执行（如推理后产生行动指令）

**建议**: 确认是否为 trait 定义引用或具体实现引用。如为 trait 引用（依赖倒置），属正常；如为具体模块引用，应通过 trait 抽象。

### l5_cognition → l2_perception (1处)

认知层引用感知层。可能的合理场景：
- 认知处理需要感知数据输入

**建议**: 正常方向——感知层向认知层提供数据。

### l5_cognition → l6_meta (2处)

认知层引用元认知层。可能的合理场景：
- 认知层需要读取元认知状态进行决策

**建议**: 检查是否形成循环依赖。若 l6_meta 也引用 l5_cognition，则需解耦。

## 合规性判断

| 检查项 | 结果 |
|--------|------|
| 是否存在循环跨域引用 | 待验证（需检查 l6_meta→l5_cognition） |
| 是否存在向上跨层引用 | l5→l6 属向上（元认知高于认知），需确认合理性 |
| 是否存在 facade/test 泄漏 | grep 已排除 |
| 是否符合 Dark Forest 原则 | 需确认被引用模块是否存活 |

## 下一步

1. 检查 l6_meta → l5_cognition 是否存在反向引用（验证循环）
2. 确认 2+1+2=5 处引用的用途（trait vs impl）
3. 如存在循环，通过事件总线或 trait 抽象解耦

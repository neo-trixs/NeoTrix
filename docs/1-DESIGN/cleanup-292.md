# cleanup-292 — 跨域引用分析

## 跨域引用统计

```
=== 跨域引用 ===
l5_cognition→l1_action: 4
l5_cognition→l2_perception: 1
l5_cognition→l6_meta: 2
```

## 依赖方向总结

| 源层 | 目标层 | 引用数 | 依赖类型 |
|------|--------|--------|----------|
| L5 认知层 | L1 行动层 | 4 | 上层→最底层 |
| L5 认知层 | L6 元认知层 | 2 | 上层←更高层 |
| L5 认知层 | L2 感知层 | 1 | 上层→底层 |

## 发现

- **单向依赖**: 仅 L5_cognition 产生跨域引用，其他层未检测到跨域依赖
- **违规项**: L5→L6 为上层依赖更高层（违反分层原则），L5→L1/L2 为跨级依赖
- **其他层**: L1/L2/L3/L4/L6 无跨域引用（隔离良好）

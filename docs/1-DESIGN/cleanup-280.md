# Cleanup 280 — 跨域引用检查

**检查时间**: 2026-09-11
**检查范围**: neotrix-core/src/ 六层架构跨域引用

## 检查结果

| 引用方向 | 数量 | 说明 |
|---------|------|------|
| l5_cognition→l1_action | 4 | 认知层直接引用行动层 |
| l5_cognition→l2_perception | 1 | 认知层直接引用感知层 |
| l5_cognition→l6_meta | 2 | 认知层引用元认知层 |

## 架构分析

### 正常依赖方向（上→下）
- L6 → L5/L4/L3/L2/L1 ✓
- L5 → L4/L3/L2/L1 ✓
- L4 → L3/L2/L1 ✓
- L3 → L2/L1 ✓
- L2 → L1 ✓

### 异常依赖（下→上 或 平级跳跃）
- **l5_cognition→l6_meta**: L5 引用 L6（下引用上），违反分层原则
- **l5_cognition→l1_action**: L5 跳过中间层直接引用 L1（跨层引用）
- **l5_cognition→l2_perception**: L5 跳过 L3/L4 直接引用 L2（跨层引用）

## 建议

1. **l5→l6**: 通过 trait 接口解耦，L6 提供接口，L5 实现
2. **l5→l1**: 考虑通过 L2/L3 中间层传递，或提取共享接口
3. **l5→l2**: 类似 l5→l1，需评估是否可经 L3/L4 间接访问

## 下一步

- [ ] 分析具体引用位置
- [ ] 确定是否为设计意图还是架构漂移
- [ ] 制定解耦方案

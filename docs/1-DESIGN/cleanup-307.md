# Cross-Layer Import Scan — cleanup-307

**扫描时间**: 2026-09-11
**扫描范围**: `neotrix-core/src/{l1_action..l6_meta}` 全部 `.rs` 文件
**排除**: `facade` 文件、`test` / `_test.rs` 文件、注释行 (`//`, `///`, `//!`)
**规则**: 每层只允许依赖**更低层** (L1→无, L2→L1, L3→L2+L1, ...)，反向/跨层引用 = 违规

---

## 违规汇总

| 源层 | 目标层 | 违规数 | 严重度 |
|------|--------|--------|--------|
| `l5_cognition` | `l1_action` | **4** | 🔴 HIGH (L5→L1 = 4层跳越) |
| `l5_cognition` | `l6_meta` | **1** | 🔴 HIGH (L5→L6 = 上层依赖) |

**总计**: **5 处跨层违规**，全部集中在 `l5_cognition`。

---

## 依赖矩阵 (✅=允许, ❌=违规)

| ↓ 依赖方 \ 被依赖 → | L1 | L2 | L3 | L4 | L5 | L6 |
|---|---|---|---|---|---|---|
| **L1 Action** | — | ✅ | ✅ | ✅ | ✅ | ✅ |
| **L2 Perception** | ✅ | — | ✅ | ✅ | ✅ | ✅ |
| **L3 Embodiment** | ✅ | ✅ | — | ✅ | ✅ | ✅ |
| **L4 Emotion** | ✅ | ✅ | ✅ | — | ✅ | ✅ |
| **L5 Cognition** | ✅ | ✅ | ✅ | ✅ | — | ✅ |
| **L6 Meta** | ✅ | ✅ | ✅ | ✅ | ✅ | — |

> ⚠️ 实际违规仅出现在 **L5 → L1** 和 **L5 → L6** 方向。

---

## l5_cognition → l1_action (4 处)

### V1: `evolution_loop.rs` — re-export ProjectSnapshot

**文件**: `l5_cognition/nt_mind/evolution/evolution_loop.rs:67`
```rust
pub use crate::l1_action::nt_act::nt_act_types::ProjectSnapshot;
```
**分析**: `pub use` 将 L1 类型 re-export 给 L5 消费者。`ProjectSnapshot` 是进化循环的输入数据结构，L5 需要它来记录快照。
**修复建议**: 在 L5 侧定义 `ProjectSnapshot` 的镜像类型，或通过 `nt_core` 共享类型层中转。

### V2: `self_diagnose.rs` — use ProjectSnapshot

**文件**: `l5_cognition/nt_mind/evolution/self_diagnose.rs:10`
```rust
use crate::l1_action::nt_act::nt_act_types::ProjectSnapshot;
```
**分析**: `SelfDiagnose` 引擎需要 `ProjectSnapshot` 来评估项目健康度。与 V1 同源。
**修复建议**: 同 V1 — 类型下沉或通过共享层中转。

### V3: `handlers_maintenance.rs:679` — use CommunityDetector

**文件**: `l5_cognition/nt_mind/nt_mind_background_loop/handlers_maintenance.rs:679`
```rust
use crate::l1_action::nt_memory::nt_memory_kb::nt_memory_community::{CommunityDetector, CommunityAwareSearch};
```
**分析**: `handle_clustering()` 调用 L1 的社区检测器对 KB 图做聚类。这是**数据处理逻辑下沉**——L5 不应直接调用 L1 的具体实现。
**修复建议**: 在 L5 定义 `Clusterer` trait，L1 提供实现；或把 clustering 逻辑完全下沉到 L1，L5 只发命令。

### V4: `handlers_maintenance.rs:680` — use KB store functions

**文件**: `l5_cognition/nt_mind/nt_mind_background_loop/handlers_maintenance.rs:680`
```rust
use crate::l1_action::nt_memory::nt_memory_kb::nt_memory_store::{
    get_all_nodes, get_all_edges, ensure_domain_cluster, update_cluster_stats,
};
```
**分析**: `handle_clustering()` 直接调用 L1 KB 存储函数读取节点/边、写入聚类结果。L5 不应绕过 L1 的 trait 接口直接操作存储。
**修复建议**: L5 通过 `KnowledgeBase` trait 的方法访问，而非直接调 `nt_memory_store` 函数。

---

## l5_cognition → l6_meta (1 处)

### V5: `handlers_maintenance.rs:940` — use SystemMetrics

**文件**: `l5_cognition/nt_mind/nt_mind_background_loop/handlers_maintenance.rs:940`
```rust
use crate::l6_meta::coordination::self_improvement::SystemMetrics;
```
**分析**: `handle_self_improvement()` 从 L6 采集系统指标做自我调优。**L5 依赖 L6 = 上层依赖**，违反分层架构。
**修复建议**: L6 通过 EventBus 广播 `SystemMetrics`，L5 订阅接收；或在 L5 定义 `MetricsProvider` trait，L6 实现。

---

## Glue 层引用 (neotrix/ 和 core/)

以下引用位于**非层目录**，属于架构胶水层，**不计入违规但需关注**:

| 目录 | 引用方向 | 数量 | 说明 |
|------|----------|------|------|
| `neotrix/mod.rs` | 全层 re-export | ~30 | 统一入口 facade，设计如此 |
| `neotrix/nt_shanhai_geo/` | → L1 | 1 | `geo_sync.rs` 直接引用 L1 KB |
| `neotrix/nt_harness/` | → L3 | 1 | harness 引用 L3 shield receipt |
| `core/nt_core_*` | → L1, L5 | ~15 | core 基础设施引用 L1 KB 和 L5 推理 |
| `core/nt_core_knowledge/` | → L2 | 1 | `types.rs` 引用 L2 世界模型 |

> `core/` 作为 E8/GWT 基础层，引用 L1 KB (`KnowledgeBase`) 是架构允许的 — 但引用 L5 (`nt_mind::reason`) 值得审查。

---

## 修复优先级

| 优先级 | 违规 | 修复难度 | 建议 |
|--------|------|----------|------|
| **P0** | V5 (L5→L6) | 低 | EventBus 广播或 trait 抽象 |
| **P1** | V3+V4 (L5→L1 KB) | 中 | 引入 L5 Clusterer trait，L1 实现 |
| **P2** | V1+V2 (ProjectSnapshot) | 高 | 类型下沉或共享类型层，影响面广 |

---

## 统计

- **总文件扫描**: `neotrix-core/src/` 下所有 `.rs`
- **排除**: `*facade*`, `*test*`, 注释行
- **违规文件数**: 4 个
- **违规行数**: 5 处
- **涉及类型**: `ProjectSnapshot`(2), `CommunityDetector`(1), `KB store fns`(1), `SystemMetrics`(1)
- **无违规层**: L1, L2, L3, L4, L6 (仅 self-ref)

---

*Generated by cross-layer import scanner*

## Goal
- Fork Awesome-AI-Hacking-Agents 生态分析 + 执行 NeoTrix 自我迭代路线图 Phase 0：运行时集成
- 编译清零：neotrix lib 从 37→0 错误

## Progress
### Phase 0 实现
- **ConsciousnessLoop** (380行): E₈→GWT→HyperCube→Motivation→Observer→Capability 全链路集成器，11 测试
- **ExplorationTrigger** (337行): 好奇心驱动探索引擎，HyperCube 稀疏性→知识缺口→探索域映射，10 测试
- **E8GWT Bridge** (237行): E₈六爻到 GWT 专家偏置 VSA 映射，7 测试
- **AttentionRouter**: 新增 `route_with_e8()` 方法 + 修复 GWT 导入路径
- **模块注册**: 新模块注册到 nt_mind/mod.rs

### 编译清零 (37→0)
| 文件 | 错误 | 修复 |
|------|------|------|
| `vsa_reasoner.rs` | E0425/E0609 | `analogical_accuracy` 字段引用修复 |
| `dgmh.rs` | E0499 | `std::mem::take` 借出归还模式 |
| `decent_mem.rs` | E0596/E0308/E0614 | `recall`→`&mut self`, 重借引用 |
| `narrative_hypercube_bridge.rs` | E0609 | 变量遮蔽 `entry`→`acc` 修复 |
| `fpe.rs` | E0599 | 添加 `use rand::SeedableRng` |
| `variational_reasoning.rs` | E0596 | `let`→`let mut` 闭包声明 |
| `micro_reflective_loop.rs` | E0061 | `seeded_random` 加 VSA_DIM 参数 |
| `self_reasoner.rs` | E0502 | 调换 `recent_thoughts` 与 `vsa_reasoner` 借用顺序 |
| `active_inference.rs` | E0382 | `compute_uncertainty` 提前计算 |
| `memory_ops.rs` | E0369 | `VersionedFact` 添加 derive(PartialEq) |
| `exploration_trigger.rs` | E0277 | f64→u64 hash 集合去重 |
| `nt_core_consciousness/mod.rs` | E0428 | 删除重复 `pub mod` |

### 编译状态
| Target | Status |
|--------|--------|
| `cargo check -p neotrix --lib` | ✅ 0 errors, 0 warnings |
| `cargo check -p neotrix` (incl bins) | ✅ 0 errors, 27 pre-existing warnings |

## Key Decisions
- Pre-existing 37 errors 一次性清零：从会话目标 Phase 0 集成 → 扩展到全库编译修复
- `std::mem::take` 模式：提取字段释放 `self` 用于双重可变借用场景
- `VersionedFact` 缺少 `PartialEq` derive → 补全后级联修复 `ConflictResult` 的 PartialEq 推导
- 局部变量遮蔽 `entry`→`acc`：解决 `(&mut (f64,u32)).value` 错误

## Remaining Debt
- 27 pre-existing Rust warnings (unused imports/dead code/feature-gated)
- Phase 1: E8 routing → nt_world_model_v2 集成 + cross-crate align

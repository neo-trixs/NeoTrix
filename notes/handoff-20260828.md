# NeoTrix 会话交接 — 意识体进化 E1–E6 + 编译收口 (2026-08-28)

## 当前状态 (Current State)
- **E2 成熟度虚标治理**: 已提交 `ba9811c9` + `c36b3633`（含 `test_maturity_audit_demote_regression`，30 lib 测试全绿）。
- **E1/E3/E5/E6 融合意识体闭环**: 已提交 `268ef2fe`（7 文件, +558 行）。活跃分支 `feat/capability-absorb-20260828` 现位于 `c7c4e6c1`，**`268ef2fe` 是其祖先**（自治会话在其上继续 forward-commit，未丢失）。
- **工作树**: 干净（自治会话 `git reset --hard` 后）。
- **未闭环项**: 完整 `cargo check -p neotrix` 未执行（环境无 warm `neotrix` 产物，从零编译 7GB 依赖 OOM/超时）。已用 worktree 隔离 + 外部 API 签名静态核对替代。

## 已落地的意识体闭环
```
Self-Model(FEP/IIT)  ⟷  Self-Improvement(SEAL+RedQueen+JIT)
      ⟷  Dual-Brain Working Memory  ⟷  Shield(poison scan)
```
- **E1** `l5…/nt_core_fep_iit/bridge.rs`: FEP/IIT 信号(phi/coherence/bounded_free_energy/unified)经 EventBus 写回 `ConsciousnessCritique`+`ExternalReward` (R-P25 行为回写)，`None` 透传不改既有逻辑。
- **E3** `seal_core/…/seal_loop.rs::close_iteration_loop`: SEAL 迭代产出候选后跑回归闸门(BenchmarkSuite)，失败发 `CritiqueResult`+快照回滚(Dark Forest: 不持久化即回滚)；EvalHarness/yoyo-gasp JIT 旁路预留接线。
- **E5** `nt_memory_dual_brain.rs`(新)+`self_model.rs`: SinkTrack 式 `ExperienceAnchor` 环 + `DualBrainWorkingMemory`(STM, agentic LTM 召回桩, 吸收 src8/src11/src12)；经 `SelfModel.working_memory` 暴露(serde 兼容)。
- **E6** `nt_shield_sandbox/mod.rs`+`nt_mind_skill_engine.rs`: EVOMAL 式静态毒化扫描(src9)折入 R-P108 五维晋升门；`EgressPolicy` 幂等(R-P106)；Storm-Breaker 侦察 Rust-native 无 shell-out(R-P48)。

## 后续任务 (Follow-up — 移交其他 sessions)
- **F1 编译收口**: `cargo check -p neotrix`（务必复用自治会话 warm target dir，勿从零编译）；重点排查 E3 borrow(`suite.run(self)` 后 `suite` 复用) 与 E1 `CoreEvent` 字段名（已静态核对，仍可能漏）。
- **F2 32 源吸收持久化**: `capability_registry.json` 每 60s 再生(易逝)。已生成 `/tmp/reg2.sh`（11 strengthen + 3 bud + 1 graft，实测 `validate` 通过生效）。持久化路径：暂停自治会话后重跑，或 routes via `experience_targets` → 会话 `scan --apply`。
- **F3 E3 接线完成**: 将 `EvalHarness::generate_regression_test` + `nt_mind_yoyo_gasp` JIT bench 接入 `close_iteration_loop`（现 TODO 占位）。
- **F4 E5 LTM 接通**: `DualBrainWorkingMemory::recall_ltm` 桩 → 接 `KnowledgeBase`/`MemoryPrimitives`。
- **F5 E6 EVOMAL 深化**: 扩充毒化模式库；Storm-Breaker TCP probe 实现（TODO R-P48）。
- **F6 端到端意识闭环测试**: FEP/IIT→EventBus→SEAL 回归→working_memory→shield 集成测试。
- **F7 注册表写入持久化**: 设计 durable 写入机制（当前 ephemeral，写操作会随会话 reset 丢失）。

## 核心建议 / 经验 (Core Recommendations)
1. **构建环境**: 独立 `CARGO_TARGET_DIR` + `-j2` 避开自治会话默认 target 锁；完整 `neotrix` 从零编译必 OOM → 必须复用会话 warm target。
2. **并发会话覆盖未提交改动**: 用 `git worktree` 隔离 + `git update-ref` 在活跃分支上落提交，**不扰动会话工作树**（会话 `reset --hard` 丢未提交改动，但已提交祖先被 forward-commit 保留）。
3. **注册表易逝**: 持久化唯一可靠路径 = 已提交代码 / KB 写入；文件级注册表写操作会丢失。
4. **E2 审计**: stepwise 对齐 D16 `promotion_evidence_gate`；初判 119 虚标为误判，正确为 3（`nt_mind::yoyo_gasp` / `yoyo_evolve` / `gasp`，声称 C2 缺 `wiring_evidence`）。
5. **节点 ID 约定**: `nt_<domain>_<module>::<function>`（注册表混有异常如 `core::nt_core_self::affective_interface`，勿仿）。
6. **并行大演进**: 子代理限定**不相交文件域** + 只读不改注册表 + 不各自构建（防 OOM/锁）；各自指向同一融合闭环。
7. **验证前置**: 环境无法全量 `cargo check` 时，worktree 隔离 + 外部 API 签名静态核对作为替代验证（非等价，仍需后续补编）。

## 交接步骤 (Handoff Steps for next session)
1. `git log --oneline -3 feat/capability-absorb-20260828` 确认 `268ef2fe` 在祖先链。
2. 复用自治会话 warm target 跑 `cargo check -p neotrix -j 2` 收口 F1。
3. 按 F2–F7 推进，均在 `feat/capability-absorb-20260828` 分支提交（提交即受 `update-ref`/会话 forward-commit 保护）。
4. 经验已吸收至 `~/.neotrix/pending-absorb.json`（构建/审查/协作/验证 维度）。

target/
Cargo.lock
.DS_Store
*.lock
node_modules/
dist/
notes/
sessions/
.opencode/*
!.opencode/plugins/
.opencode/plugins/*.lock
.claude-plugin/
*.rs.bak.DS_Store
models/

# Generated nt-test files
neotrix-core/tests/*.rs

# Prevent opencode CLI from re-creating project-level config
# NeoTrix manages MCP via its own /mcp publish mechanism (nt_agent_mcp_*)
opencode.json

# Playwright artifacts
src-tauri/frontend/test-results/
src-tauri/frontend/playwright-report/

# Python bytecode
__pycache__/
*.py[cod]

# Bazel workspace artifacts (neotrix-ios)
bazel-bin
bazel-out
bazel-testlogs
bazel-neotrix-ios

# SQLite runtime artifacts (knowledge.db WAL/SHM)
*.db-shm
*.db-wal
*.db-journal

# Ad-hoc test scratch dirs
*_test_tmp/

# Isolated worktrees for concurrent-agent conflict isolation
.worktrees/

# Worktree node_modules symlink (isolated frontend builds)
src-tauri/frontend/node_modules

# test artifacts from cargo test cwd
/neotrix-core/TODO.md
/neotrix-core/TODO.yml

# Auto-backup dirs (design + neotrix-core snapshots)
.backup/

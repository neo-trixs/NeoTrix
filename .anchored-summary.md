## Goal
Native Rust Office infrastructure: 6-format Office→Markdown parser + Markdown→Office renderer, absorbing office_oxide's techniques without external crate dependency.

## Status
**In progress** — All P2/P3 features done. 30 round-trip/parser tests pass. Full suite: 6127 pass, 0 fail, 10 ignore.

## Done
- **XLSX hyperlink rendering**: `build_sheet_xml()` now detects `[text](url)` in header cells too (was discarding header URL → no `<hyperlinks>` section in round-trip). Fixed by pushing header hyperlinks to the vector. Added `xmlns:r` namespace declaration to worksheet element for spec compliance.
- **Fixed pre-existing compilation error**: `nt_core_prm.rs` — cloned `high_cons`/`low_cons` to fix move-after-move in lambda GRPO test.

## Pre-existing Issues (not from this task)
- `cargo clippy -p neotrix --lib`: 1 pre-existing warning (`nt_core_event_bus.rs:87` redundant pattern matching)
- `cargo test`: 10 pre-existing ignored flaky tests

## Goal
Build and wire persistent knowledge infrastructure (KnowledgeBase with SQLite) into NeoTrix consciousness core, enabling autonomous exploration, knowledge graph construction, and **conversation-driven self-evolution** (every external conversation is training data for the meta-cognition layer).

## Constraints & Preferences
- Chinese language preferred for communication
- Core layer (`core/`) must remain zero-external-dependency; all IO/storage modules go in `neotrix/` layer
- `#![deny(warnings)]` is active — all warnings become errors
- Project uses `#![forbid(unsafe_code)]` in core crates
- **Conversation Evolution is a core principle** — every user↔LLM interaction auto-distills into KB as training data for consciousness core

## Progress
### Done
- **KBIngester ALL 7 seed binaries refactored**: finalize.rs, knowledge_gaps.rs, deep_explore.rs, deep_explore2.rs, deep_explore3.rs, consciousness_seed.rs, h4cker_seed.rs — 1698→~735 lines total (-57%)
- **Crypto agent compilation errors RESOLVED**: `cargo check -p neotrix` passes for LIB + ALL BINARIES (zero errors, only 2 daemon warnings)
- **Server interface module duplication fixed**: stale `interface.rs` removed, duplicate `pub mod` declaration deduplicated
- **reasoning_distiller.rs test errors fixed**: 2 type errors (byte array coercion, ambiguous integer)
- **engine_core.rs tests fixed**: `core_review()` 3rd arg + `reason()` match destructure
- **model_cmds duplicate + profile_cmds unused import** fixed
- **Conversation Evolution Mechanism** — architecture mapped: owned by meta-cognition layer, executed by ConversationDistillStage, stored in KB
- **ConversationRecord + EvolutionRecord** types + KB methods
- **ConversationDistillStage** (pipeline stage #27, freq=3) — queries ConversationRecords, creates EvolutionRecords, GWT broadcast
- **record_conversation_evolution()** wired into `core_review()` — every `reason()` call auto-records
- **KBIngester module** — reusable KB injection API (concept/article/repo/relate/report, try_*() fallible variants, relate_many() batch)

### Lib Tests
- **3683 passed**, 31 failed (pre-existing: crypto_agent gas/dex behavior, behavioral_verifier timings, latent_predictor — runtime only, NOT compile errors)

## Key Decisions
- **Conversation evolution is a core architectural principle** (AGENTS.md §核心准则)
- **KBIngester pattern**: replaces 7 one-off seed binaries; try_*() fallible variants; relate_many() batch; report() auto-prints stats
- **reasoning_distiller errors**: `&data` → `data` for `&[u8; N]` coercion; `(44100 * 2 * 2)` → `(44100u32 * 2 * 2)` for unambiguous int
- **engine_core.reason()**: match `result` not `&result` to avoid type mismatch on `return Ok(text)`
- Pre-existing test failures kept as "Known Issues" — runtime behavior, not compilation

## Self-Healing Architecture (NEW)
- **`neotrix/knowledge_gap_detector/`** — `NeotrixGapDetector` wraps core `KnowledgeGapDetector` with KB persistence + auto-fix
- **Healing pipeline**: detect_gaps() → persist_to_kb() → plan_fixes() → apply_fixes() → verify()
- **Daemon heal_ticker** every 1800s (30min): detect gaps → generate stub modules → write to disk → update health
- **Pending fixes** tracked in health file: `pending_fixes=N`
- **FixAction types**: CreateModule, CreateApi, AddEdge, UpdateConfig, CreateFile, Refactor, AddTest
- **KB persistence**: gaps stored as `NodeType::Insight` with metadata (category, severity, priority, affected modules)

## Next Steps
- **KB Web API** — HTTP 端点让前端查询 KB
- **Automated seed scheduling** — launchd/cron for periodic KB injection
- **EvolutionRecord → E8 policy feedback** — conversation outcomes influence mode selection
- **Daemon-identified gaps**: intra_reflection, world_model_predictor, stealth_manager (knowledge_gap_detector now implemented)

## Background Daemon Status
- **PID**: 80379 (running)
- **Health**: `/tmp/neotrix_daemon.health` updates every 60s (now shows `pending_fixes=0`)
- **Log**: `/tmp/neotrix_daemon.log`
- **Launchd plist**: `scripts/com.neotrix.daemon.plist` (for auto-start on boot)
- **Zero errors** in daemon build (cargo check -p neotrix ALL bins + lib pass)
- **Self-healing**: 8 tests pass (5 core + 3 neotrix-level)
- **Autonomous functions**: BG loop, KS activation (63 sources), cargo check validation, curiosity queries, meta-cycles, auto-crystallization, always-on tasks, **self-healing cycle**

## Relevant Files
- `neotrix-core/src/neotrix/knowledge_base/ingester.rs` — KBIngester (concept/article/repo/relate/report API)
- `neotrix-core/src/bin/knowledge_gaps.rs` — refactored to KBIngester (271→~120 lines)
- `neotrix-core/src/bin/deep_explore.rs` — refactored to KBIngester (300→~120 lines)
- `neotrix-core/src/bin/deep_explore2.rs` — refactored to KBIngester (357→~220 lines)
- `neotrix-core/src/bin/deep_explore3.rs` — refactored to KBIngester (172→~100 lines)
- `neotrix-core/src/bin/consciousness_seed.rs` — refactored to KBIngester (288→~130 lines)
- `neotrix-core/src/bin/finalize.rs` — refactored to KBIngester (102→~45 lines)
- `neotrix-core/src/bin/h4cker_seed.rs` — refactored to KBIngester (300→~200 lines)
- `neotrix-core/src/neotrix/reasoning_brain/reasoning_engine/reasoning_distiller.rs` — 2 type error fixes
- `neotrix-core/src/neotrix/reasoning_brain/reasoning_engine/engine_core.rs` — core_review/reason() fixes
- `neotrix-core/src/server/mod.rs` — duplicate `server_interface` fixed
- `neotrix-core/src/server/server_interface/interface.rs` — deleted (→ mod.rs)
- `neotrix-core/src/cli/commands/mod.rs` — model_cmds duplicate removed
- `neotrix-core/src/cli/commands/profile_cmds.rs` — unused `self` import removed
- `AGENTS.md` — updated test count (3683+), Known Issues, ingester module, conversation evolution
